export enum RouterPaths {
  DASHBOARD = '/',
  PRODUCTS = '/products',
  USERS = '/users',
  LOGIN = '/login',
  REGISTER = '/register'
}

export enum RouterNames {
  DASHBOARD = 'dashboard',
  PRODUCTS = 'products',
  USERS = 'users',
  LOGIN = 'login',
  REGISTER = 'register'
} 